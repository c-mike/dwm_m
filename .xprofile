#! /bin/bash

if [ "$XDG_CURRENT_DESKTOP" = "GNOME" ];
then
   # img de funco
   nitrogen --restore &

else
   echo "Nao e GNOME"
fi

# status bar "dwm-bar"
# /home/c-mike/Git/dwm-bar/dwm_bar.sh &

# Statusbar loop
#while true; do
   #xsetroot -name "$( date +"%F %R" )"
   #sleep 1m    # Atualiza a cada minuto
#done &

#dwmstatus
#dwmstatus 2>&1 >/dev/null &
